export default {
	/**
	 * Disable the transition and end the animation immediately.
	 *
	 * Useful when the transition is not expected to be performed in certain specific states.
	 */
	disabled: false,
};
